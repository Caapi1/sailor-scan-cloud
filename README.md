# sailor-scan-cloud

Build repo for the Sailor Fields cloud scan container (Lane B): ffmpeg + COLMAP 3.11.1 (CUDA) + OpenSplat (CUDA) + a RunPod serverless handler. Open-source pipeline binaries and a generic handler only — no data, no keys.

Canonical source: `tools/scan-pipeline/cloud/` in the Sailor repo.
